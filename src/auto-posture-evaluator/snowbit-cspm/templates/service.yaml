apiVersion: v1
kind: Service
metadata:
  name: {{ include "snowbit-cspm.fullname" . }}
  labels:
    {{- include "snowbit-cspm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "snowbit-cspm.selectorLabels" . | nindent 4 }}
